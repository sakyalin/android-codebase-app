<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.linxinzhe.android.codebaseapp"
          xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <!-- 扫码 -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- bugly -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.READ_LOGS"/>

    <!-- bugly -->
    <!-- umeng -->
    <!-- umeng -->
    <application
        android:name=".MyApplication"
        android:allowBackup="false"
        tools:replace="android:allowBackup"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:value="5743f8bbe0f55aeaae003013"
            android:name="UMENG_APPKEY"/>
        <meta-data
            android:name="UMENG_CHANNEL"
            android:value="${UMENG_CHANNEL_VALUE}"/>

        <activity
            android:name=".feature.SplashActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".feature.GuideActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".feature.MainActivity">
        </activity>
        <activity android:name=".feature.ViewPagerTabsActivity">
        </activity>
        <activity android:name=".feature.RefreshAndLoadMoreActivity">
        </activity>
        <activity android:name=".feature.QrScanActivity">
        </activity>
        <activity android:name=".feature.UploadImgActivity">
        </activity>
    </application>

</manifest>